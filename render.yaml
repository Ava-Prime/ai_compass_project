services:
  - type: web
    name: gpt-runtime
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.gpt_runtime:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true

